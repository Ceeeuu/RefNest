# Copy this file to .env and fill in real values. .env is git-ignored.
SECRET_KEY=change-me

# PostgreSQL (local install)
DB_NAME=refnest
DB_USER=postgres
DB_PASSWORD=your-postgres-password
DB_HOST=localhost
DB_PORT=5432

# OpenAI (used from Stage 3 onward)
OPENAI_API_KEY=sk-...
